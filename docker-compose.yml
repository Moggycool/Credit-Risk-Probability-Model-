version: "3.8"

services:
  ptv-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ptv-api
    ports:
      - "8000:8000"
    environment:
      MODEL_LOCAL_PATH: /app/models/logistic_best.pkl
      MODEL_STAGE: production
      MODEL_NAME: ""
    volumes:
      - ./models:/app/models:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"